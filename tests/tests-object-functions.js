var nodeunit = require('nodeunit');
var arrays = require('../src/object-functions');

exports['Values'] = nodeunit.testCase({

'an empty set of values': function (test) {
         //test.equal(arrays.Values());
         //test.done();
});

